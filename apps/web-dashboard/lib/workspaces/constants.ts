/** Cross-page keys — keep in sync with agents, memory, and other consumers. */
export const STORAGE_PROJECT = 'dd.project_key';
export const STORAGE_WORKSPACE = 'dd.workspace_key';

/** Versioned layout blob for Workspaces page only. */
export const STORAGE_WORKSPACES_UI = 'dd.workspaces_ui_v1';
