/** HttpOnly cookie storing Keycloak access_token (JWT). */
export const ACCESS_TOKEN_COOKIE = 'dd_access_token';

/** HttpOnly cookie storing Keycloak refresh_token (opaque or JWT depending on realm). */
export const REFRESH_TOKEN_COOKIE = 'dd_refresh_token';
