[project]
name = "browser-runner"
version = "0.1.0"
description = "Playwright-backed web screenshot, extract, and crawl service"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
